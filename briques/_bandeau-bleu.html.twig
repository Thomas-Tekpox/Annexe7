<div class="container-fluid bg-sb-blue-m">
	<div class="container py-5">
		<div class="row">
			<div class="col-md-3">
				<img src="{{ asset(content.logo.url) }}" height="125" alt="{{ content.logo.alt }}">
			</div>
			<div class="col-md-4">
				<p class="{{ content.desc.color }} fs-3">{{ content.desc.text }}</p>
			</div>
			<div class="col-1"></div>
			<div class="col-md-4">
				<h2 class="{{ content.title.color }} h4 fw-bold">{{ content.title.text }}</h2>
				<p class="{{ content.moto.color }} h2 fw-bold">{{ content.moto.text }}</p>
				<p class="{{ content.desc2.color }} fs-4 pt-2 fw-bold">{{ content.desc2.text }}<span class="{{ content.desc2.color2 }}">{{ content.desc2.span }}</span></p>
			</div>
		</div>
	</div>
</div>